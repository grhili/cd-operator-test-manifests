replicaCount: 1

image:
  repository: hashicorp/http-echo
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5678

resources:
  limits:
    cpu: 30m
    memory: 32Mi
  requests:
    cpu: 5m
    memory: 16Mi

args:
  - "-text=API Backend Service"
