apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: notification-service
  labels:
    app: notification-service
    environment: dev
    version: 1.6.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        version: 1.6.0
    spec:
      containers:
      - name: notification-service
        image: busybox:1.36
        command: ['sh', '-c', 'echo Notification Service v1.6.0 started && sleep 3600']
        env:
        - name: ENVIRONMENT
          value: "dev"
        - name: VERSION
          value: "1.6.0"
        resources:
          requests:
            memory: "24Mi"
            cpu: "35m"
          limits:
            memory: "48Mi"
            cpu: "75m"
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Namespace
metadata:
  name: notification-service
