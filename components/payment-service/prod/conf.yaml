apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-service
  labels:
    version: 1.0.0
    # Missing closing quote
    environment: prod
spec:
  replicas: [invalid syntax here
  containers:
  - name: test
