apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-validator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: data-validator
  template:
    metadata:
      labels:
        app: data-validator
        version: 1.0.0
        cluster: cluster-us-east-1
    spec:
      containers:
      - name: data-validator
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
