apiVersion: apps/v1
kind: Deployment
metadata:
  name: text-to-speech
spec:
  replicas: 2
  selector:
    matchLabels:
      app: text-to-speech
  template:
    metadata:
      labels:
        app: text-to-speech
        version: 1.0.0
        cluster: cluster-us-west-2
    spec:
      containers:
      - name: text-to-speech
        image: nginx:1.25-alpine
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
