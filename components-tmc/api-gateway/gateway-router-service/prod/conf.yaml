apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-router-service
  namespace: api-gateway
  labels:
    app: gateway-router-service
    component-group: api-gateway
    environment: prod
    version: 1.8.5
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gateway-router-service
  template:
    metadata:
      labels:
        app: gateway-router-service
        version: 1.8.5
    spec:
      containers:
      - name: gateway-router-service
        image: httpd:2.4-alpine
        ports:
        - containerPort: 80
        env:
        - name: ENVIRONMENT
          value: "prod"
        - name: VERSION
          value: "1.8.5"
        - name: SERVICE_NAME
          value: "gateway-router-service"
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-router-service
  namespace: api-gateway
spec:
  selector:
    app: gateway-router-service
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
